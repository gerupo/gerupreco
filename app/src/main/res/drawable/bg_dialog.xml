<?xml version="1.0" encoding="utf-8"?>
<!-- Window background for framework dialogs (ProgressDialog / AlertDialog) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/surface_container" />
    <corners android:radius="@dimen/radius_xl" />
    <stroke
        android:width="@dimen/card_stroke"
        android:color="@color/outline_variant" />
</shape>
